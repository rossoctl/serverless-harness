{
  "name": "serverless-harness",
  "private": true,
  "description": "Workspace root. Holds only repo-wide tooling -- runtime code lives in harness/ and packages/*.",
  "scripts": {
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.9.8"
  }
}
